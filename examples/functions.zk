fn greet(name) {
    print("Hello " + name)
}

greet("Prabhashvara")

fn add(a, b) {
    return a + b
}

let result = add(10, 20)
print(result)

fn factorial(n) {
    if n <= 1 {
        return 1
    }

    return n * factorial(n - 1)
}

print(factorial(5))
